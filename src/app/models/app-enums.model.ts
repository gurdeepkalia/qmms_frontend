export enum DataType{
    VARCHAR2 = "VARCHAR2",
    NUMBER = "NUMBER",
    DATE = "DATE",
    CHAR = "CHAR",
    LONG = "LONG"
}

export enum Frequency {
    DAILY = "DAILY",
    WEEKLY = "WEEKLY",
    YEARLY = "YEARLY",
    MONTHLY = "MONTHLY"
}

export enum Status{
    ENABLE = "ENABLE",
    DISABLE = "DISABLE"
}